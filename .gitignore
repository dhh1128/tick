__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/

# zipapp build artifact (built into dist/, which is ignored above)
*.pyz

# tick's own ledger worktree, created by dogfooding `tick init`
/.tick
.tick.lock
